# teste-isportistics
Para desenvolver utilizei a IDE Visual Studio Code, e para rodar o projeto precisei do xampp, que roda um servidor local, foi necessário
para o PHP e MySql.
Utilizei o npm para utilizar o bootstrap. Para rodar o programa é necessário ter as duas ferramentas que citei acima.

"npm install" para instalar as dependências (bootstrap).
